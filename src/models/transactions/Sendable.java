package models.transactions;

// This is to reproduce the same UI layout for items of transactions between user-user, and user-business
// Any field that should is not used, an empty string value can be added to avoid exception

public interface Sendable {

}
